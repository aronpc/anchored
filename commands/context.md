---
description: Load persistent cross-tool memory for the current project (identity, recent decisions, relevant entities)
---

Load Anchored memory for this project.

Run the `anchored_context` MCP tool with the current working directory. Then summarize what came back to the user — identity, project context, recent decisions and any relevant memories — so they know what's already known about them and the project before continuing.

If the MCP server is not connected, run `anchored doctor` to diagnose and report what's wrong.
